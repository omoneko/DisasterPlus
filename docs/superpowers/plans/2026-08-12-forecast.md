# ①天気予報タブ 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** バニラが既に持つ災害ハザードのヒートマップを 1 クリックで出せるようにし、バニラに無い「時間軸」（天候の傾向とクールダウン）とハザード値の数値化を足した予報パネルを提供する。

**Architecture:** ヒートマップは**自前で描かない** — `InfoManager.SetCurrentMode` でバニラのものへ切り替える。`Core/Forecast/` に傾向判定・段階写像・方位変換（全てユニットテスト対象）、`Game/Forecast/` に `WeatherManager` / `DisasterManager` の読み取りとパネル UI。読み取りは sim スレッド、UI は main スレッドで、Phase 0.5 と同じ snapshot-then-render で渡す。ボタン位置は固定せず、実行時に空き矩形を走査する。

**Tech Stack:** C# 7.3 / .NET Framework 3.5、Unity 5.6、ColossalFramework UI、xunit / net8.0（Core テスト）、MSBuild。

**Spec:** `docs/superpowers/specs/2026-08-12-forecast-design.md`

## Global Constraints

- **.NET Framework 3.5 / C# 7.3**、Unity 5.6。C# 8+ 構文（switch 式、`??=`、範囲演算子、`using` 宣言、null 許容参照型）は**コンパイルエラー**になる
- Unity 5.6 以降で追加された API は存在しない。使う前に `docs/tools/ilload.ps1` で実アセンブリを確認する
- `Core/` は `UnityEngine`・CS API・`System.Random`・**LINQ** を参照しない。`System.Collections.Generic` と `System.Math`（＋`System.Text.StringBuilder`）まで
- net35 に `System.Collections.Concurrent` は無い。共有状態は単一の `lock`
- **スレッド境界**: `WeatherManager` / `DisasterManager` / 建物・車両・災害バッファの読み取りは sim スレッド（`OnSimulationTick`）。`UIComponent`・`InfoManager.SetCurrentMode`・GameObject は main スレッドのみ。破るとスタックトレース無しの `IndexOutOfRangeException` ポップアップが後から出る
- 全ての `Game/` ファイルは、サブフォルダに関わらず名前空間 `DisasterPlus.Game`
- **`Assembly-CSharp` はグローバル名前空間に `LogChannel` enum を宣言している。** こちらの `LogChannel` は常に完全修飾する
- `Log.Diag(key, msg)` のキーは短い静的文字列。エンティティごとの動的文字列は禁止
- 全セッション状態はレベルアンロードでリセットする
- 保存設定は公開契約。`LogChannel` のビット値と既存の設定キーを詰め直さない
- `Strings` / `Locales/en.txt` / `Locales/ja.txt` のキー集合は常に一致させる（現在 **26**）。追加したら `tools/GenerateLocaleTemplate.ps1` で `en.txt` を再生成する
- `Locales/*.txt` は UTF-8 **BOM なし**。日本語コメントを含む `.ps1` は UTF-8 **BOM あり**
- **PowerShell の `Get-Content -Raw` → `Set-Content` でファイルを編集しない**（日本語が化ける）。Write/Edit ツールを使う
- コミットは `<type>: <説明>` 形式（feat/fix/docs/test/chore/refactor）。本文は日本語で可

## IL 実測済みの事実（推測で上書きしないこと）

設計書 §1.2 / §3 より。**全て実アセンブリで確認済み。**

- `InfoManager.SetCurrentMode(InfoMode mode, SubInfoMode subMode)` は **public**
- `InfoManager.InfoMode.DisasterHazard` = 29
- `InfoManager.SubInfoMode` に `LightningHazard` / `TornadoHazard` / `EarthquakeHazard` / `MeteorHazard` / `SinkholeHazard` / `ForestFireHazard` / `FloodHazard` が存在
- `ThunderStormAI.UpdateHazardMap`（237命令）/ `TornadoAI.UpdateHazardMap`（335命令）は実装済み＝**ハザードマップは実際に埋まる**
- `DisasterManager.SampleDisasterHazardMap` / `m_hazardTexture` / `HAZARDMAP_RESOLUTION` / `HAZARDMAP_CELL_SIZE` が存在
- `WeatherManager` の public フィールド: `m_currentRain` / `m_targetRain` / `m_currentCloud` / `m_targetCloud` / `m_currentFog` / `m_targetFog` / `m_currentTemperature` / `m_targetTemperature` / `m_windDirection` / `m_targetDirection` / `m_groundWetness` / `m_lastLightningIntensity` / `m_windGrid` / `WINDGRID_RESOLUTION`
- `DisasterManager.m_randomDisastersProbability` / `m_randomDisasterCooldown` が存在
- `ColossalFramework.UI.UIView.GetAView()` は static。`UIComponent` は `absolutePosition`（Vector3）/ `size`（Vector2）/ `isVisible`（bool）/ `parent` を持つ

## 既存コードで前提にしてよいもの

- `Core/Common`: `Vec2`, `Vec3`
- `Core/Diagnostics`: `DiagnosticLine`, `FeatureHealth`, `AssumptionResult`, `LogChannel`（`Forecast = 4` は**定義済み・未使用**）
- `Game`: `Log`（`Info`/`Warn`/`Error`/`Diag(key,msg)`/`Diag(channel,key,msg)`）、`ModSettings`（`Ensure()`, `FileName = "DisasterPlusSettings"`）、`ModCompat`（`NdrPresent` / `NaturalDisastersOwned`、どちらもキャッシュ済み）、`Strings`, `LocaleLoader`
- `IDisasterFeature`: `Name` / `OnLevelLoaded()` / `OnSimulationTick(uint, float)` / `OnMainThreadUpdate()` / `OnLevelUnloading()` / **`WriteDiagnostics(DiagnosticBuilder)`**
- `FeatureHost`: `Register()`, `FramesPerMinute`, `NoteDegraded` / `ClearDegraded`
- `Assumptions`: `Run()` / `LastResults` / `Reset()`。検証項目は 1 ファイルに集約する方針
- `DiagnosticBuilder`: `Line(int indent, string label, string value)` / `Line(int indent, string label)`
- `SceneObjects.FindInScene<T>() where T : Component` — Unity 5.6 の `FindObjectOfType` が非アクティブを返さない問題への対処済みヘルパー

---

## File Structure

### 新規作成

```
src/DisasterPlus/Core/Forecast/
  Trend.cs                 enum Rising / Falling / Steady
  TrendMath.cs             current と target の差 → Trend（不感帯つき）
  HazardLevel.cs           0-255 → 表示段階(0-10)とバーの文字列
  WindDirection.cs         角度 → 16 方位のラベル
  ForecastReading.cs       1 災害種ぶんの不変の読み取り結果

src/DisasterPlus/Game/Forecast/
  WeatherSnapshot.cs       sim スレッドで作る不変スナップショット
  WeatherReader.cs         WeatherManager / DisasterManager の読み取り
  HazardMapReader.cs       SampleDisasterHazardMap のラッパー
  InfoModeSwitch.cs        SetCurrentMode のラッパー
  ForecastHub.cs           snapshot-then-render の受け渡し
  ForecastFeature.cs       IDisasterFeature 実装
  ForecastPanel.cs         パネル UI（main スレッド）

src/DisasterPlus/Game/UI/
  FreeSlotFinder.cs        空き矩形の走査（②以降も使う）
  ForecastPanelButton.cs   トグルボタン

tests/DisasterPlus.Core.Tests/Forecast/
  TrendMathTests.cs
  HazardLevelTests.cs
  WindDirectionTests.cs
```

### 変更

```
src/DisasterPlus/Game/Diagnostics/Assumptions.cs   予報ぶんの前提を追加
src/DisasterPlus/Game/Common/DisasterPlusLoading.cs 機能の登録
src/DisasterPlus/Game/ModSettings.cs               予報の設定キー
src/DisasterPlus/Game/Mod.cs                       予報グループ・Forecast ログチャンネル
src/DisasterPlus/Game/Localization/Strings.cs      文字列
Locales/en.txt, Locales/ja.txt                     同上
```

### 責務の境界

- `Core/Forecast` は純粋。全て不変値型か純関数
- `ForecastHub` が唯一の共有可変状態。単一の `lock`、外へは不変コピーのみ
- `FreeSlotFinder` は UI 座標計算だけを行い、ボタンのことを知らない（②以降が再利用する）
- `InfoModeSwitch` は `SetCurrentMode` を包むだけ。判断をしない

---

## Task 1: 傾向・段階・方位（Core、純関数）

**Files:**
- Create: `src/DisasterPlus/Core/Forecast/Trend.cs`
- Create: `src/DisasterPlus/Core/Forecast/TrendMath.cs`
- Create: `src/DisasterPlus/Core/Forecast/HazardLevel.cs`
- Create: `src/DisasterPlus/Core/Forecast/WindDirection.cs`
- Create: `tests/DisasterPlus.Core.Tests/Forecast/TrendMathTests.cs`
- Create: `tests/DisasterPlus.Core.Tests/Forecast/HazardLevelTests.cs`
- Create: `tests/DisasterPlus.Core.Tests/Forecast/WindDirectionTests.cs`

**Interfaces:**
- Consumes: なし
- Produces:
  - `DisasterPlus.Core.Forecast.Trend` — `enum { Steady, Rising, Falling }`
  - `TrendMath` — `static Trend Of(float current, float target, float deadband)`、`const float DefaultDeadband = 0.02f`
  - `HazardLevel` — `static int StepOf(byte hazard)`（0–10）、`static string BarOf(byte hazard)`（`▓`/`░` 10 文字）、`const int Steps = 10`
  - `WindDirection` — `static string LabelOf(float degrees)`（16 方位の英語略号 `N`,`NNE`,…,`NNW`）

**なぜ不感帯が要るか:** `WeatherManager` の current は target へ連続的に補間される。厳密比較だと常に Rising/Falling になり、矢印が意味を失う。

- [ ] **Step 1: 失敗するテストを書く**

`tests/DisasterPlus.Core.Tests/Forecast/TrendMathTests.cs`:

```csharp
using DisasterPlus.Core.Forecast;
using Xunit;

namespace DisasterPlus.Core.Tests.Forecast
{
    public class TrendMathTests
    {
        [Fact]
        public void TargetAboveCurrent_IsRising()
        {
            Assert.Equal(Trend.Rising, TrendMath.Of(0.2f, 0.8f, TrendMath.DefaultDeadband));
        }

        [Fact]
        public void TargetBelowCurrent_IsFalling()
        {
            Assert.Equal(Trend.Falling, TrendMath.Of(0.8f, 0.2f, TrendMath.DefaultDeadband));
        }

        [Fact]
        public void Equal_IsSteady()
        {
            Assert.Equal(Trend.Steady, TrendMath.Of(0.5f, 0.5f, TrendMath.DefaultDeadband));
        }

        [Fact]
        public void WithinDeadband_IsSteady()
        {
            // 補間中はごく小さな差が常に出る。これを Rising にすると矢印が無意味になる。
            Assert.Equal(Trend.Steady, TrendMath.Of(0.500f, 0.510f, 0.02f));
            Assert.Equal(Trend.Steady, TrendMath.Of(0.510f, 0.500f, 0.02f));
        }

        [Fact]
        public void ExactlyAtDeadband_IsSteady()
        {
            // 境界は Steady 側に倒す（ちらつきを減らす）
            Assert.Equal(Trend.Steady, TrendMath.Of(0.50f, 0.52f, 0.02f));
        }

        [Fact]
        public void JustBeyondDeadband_IsRising()
        {
            Assert.Equal(Trend.Rising, TrendMath.Of(0.50f, 0.5201f, 0.02f));
        }

        [Fact]
        public void NegativeValues_Work()
        {
            Assert.Equal(Trend.Rising, TrendMath.Of(-10f, -2f, 0.02f));
            Assert.Equal(Trend.Falling, TrendMath.Of(-2f, -10f, 0.02f));
        }

        [Fact]
        public void ZeroDeadband_AnyDifferenceCounts()
        {
            Assert.Equal(Trend.Rising, TrendMath.Of(0.5f, 0.5000001f, 0f));
        }

        [Fact]
        public void NegativeDeadband_IsTreatedAsZero()
        {
            // 呼び出し側の設定ミスで挙動が反転しないこと
            Assert.Equal(Trend.Rising, TrendMath.Of(0.2f, 0.8f, -1f));
        }

        [Fact]
        public void NaNInputs_AreSteady()
        {
            // 破損した読み取りで矢印が嘘をつかないこと
            Assert.Equal(Trend.Steady, TrendMath.Of(float.NaN, 0.5f, 0.02f));
            Assert.Equal(Trend.Steady, TrendMath.Of(0.5f, float.NaN, 0.02f));
        }
    }
}
```

`tests/DisasterPlus.Core.Tests/Forecast/HazardLevelTests.cs`:

```csharp
using DisasterPlus.Core.Forecast;
using Xunit;

namespace DisasterPlus.Core.Tests.Forecast
{
    public class HazardLevelTests
    {
        [Fact]
        public void Zero_IsStepZero()
        {
            Assert.Equal(0, HazardLevel.StepOf(0));
        }

        [Fact]
        public void Max_IsTopStep()
        {
            Assert.Equal(HazardLevel.Steps, HazardLevel.StepOf(255));
        }

        [Fact]
        public void StepIsMonotonic()
        {
            int prev = -1;
            for (int i = 0; i <= 255; i++)
            {
                int s = HazardLevel.StepOf((byte)i);
                Assert.True(s >= prev, "step decreased at " + i);
                Assert.True(s >= 0 && s <= HazardLevel.Steps, "step out of range at " + i);
                prev = s;
            }
        }

        [Fact]
        public void BarLengthIsAlwaysSteps()
        {
            for (int i = 0; i <= 255; i++)
            {
                Assert.Equal(HazardLevel.Steps, HazardLevel.BarOf((byte)i).Length);
            }
        }

        [Fact]
        public void BarFilledCountMatchesStep()
        {
            for (int i = 0; i <= 255; i++)
            {
                string bar = HazardLevel.BarOf((byte)i);
                int filled = 0;
                foreach (char c in bar) if (c == HazardLevel.FilledChar) filled++;
                Assert.Equal(HazardLevel.StepOf((byte)i), filled);
            }
        }

        [Fact]
        public void BarIsAsciiOnly()
        {
            // CS の UI フォントに罫線素片（▓ ░）がある保証は無く、豆腐になりうる。
            // このプロジェクトは描画前提の思い込みで既に何度か失敗している。
            for (int i = 0; i <= 255; i += 17)
            {
                foreach (char c in HazardLevel.BarOf((byte)i))
                {
                    Assert.True(c < 128, "non-ASCII character in bar: " + (int)c);
                    Assert.True(c == HazardLevel.FilledChar || c == HazardLevel.EmptyChar,
                        "unexpected char: " + c);
                }
            }
        }
    }
}
```

`tests/DisasterPlus.Core.Tests/Forecast/WindDirectionTests.cs`:

```csharp
using DisasterPlus.Core.Forecast;
using Xunit;

namespace DisasterPlus.Core.Tests.Forecast
{
    public class WindDirectionTests
    {
        [Fact]
        public void CardinalPoints()
        {
            Assert.Equal("N", WindDirection.LabelOf(0f));
            Assert.Equal("E", WindDirection.LabelOf(90f));
            Assert.Equal("S", WindDirection.LabelOf(180f));
            Assert.Equal("W", WindDirection.LabelOf(270f));
        }

        [Fact]
        public void IntercardinalPoints()
        {
            Assert.Equal("NE", WindDirection.LabelOf(45f));
            Assert.Equal("SW", WindDirection.LabelOf(225f));
        }

        [Fact]
        public void SecondaryIntercardinal()
        {
            Assert.Equal("NNE", WindDirection.LabelOf(22.5f));
            Assert.Equal("WNW", WindDirection.LabelOf(292.5f));
        }

        [Fact]
        public void WrapsAboveThreeSixty()
        {
            Assert.Equal("N", WindDirection.LabelOf(360f));
            Assert.Equal("E", WindDirection.LabelOf(450f));
            Assert.Equal("N", WindDirection.LabelOf(720f));
        }

        [Fact]
        public void HandlesNegativeAngles()
        {
            Assert.Equal("N", WindDirection.LabelOf(-360f));
            Assert.Equal("W", WindDirection.LabelOf(-90f));
            Assert.Equal("NNW", WindDirection.LabelOf(-22.5f));
        }

        [Fact]
        public void BoundariesRoundToNearestSector()
        {
            // セクタ幅は 22.5 度。境界の 11.25 度で次のセクタに入る。
            Assert.Equal("N", WindDirection.LabelOf(11.24f));
            Assert.Equal("NNE", WindDirection.LabelOf(11.26f));
        }

        [Fact]
        public void NaN_ReturnsUnknown()
        {
            Assert.Equal("?", WindDirection.LabelOf(float.NaN));
        }

        [Fact]
        public void AlwaysReturnsANonEmptyLabel()
        {
            for (float d = -720f; d <= 720f; d += 3.7f)
            {
                Assert.False(string.IsNullOrEmpty(WindDirection.LabelOf(d)));
            }
        }
    }
}
```

- [ ] **Step 2: 失敗を確認する**

```bash
dotnet test tests/DisasterPlus.Core.Tests/DisasterPlus.Core.Tests.csproj
```

Expected: コンパイルエラー `The type or namespace name 'TrendMath' could not be found`

- [ ] **Step 3: Trend と TrendMath を実装する**

`src/DisasterPlus/Core/Forecast/Trend.cs`:

```csharp
namespace DisasterPlus.Core.Forecast
{
    public enum Trend
    {
        Steady,
        Rising,
        Falling,
    }
}
```

`src/DisasterPlus/Core/Forecast/TrendMath.cs`:

```csharp
namespace DisasterPlus.Core.Forecast
{
    /// <summary>
    /// WeatherManager の current と target の差から傾向を出す。
    ///
    /// これがこの機能の中核。バニラは hazard（静的な危険度）しか見せず、
    /// 「今どちらへ向かっているか」を出さない。current/target の両方が
    /// public フィールドとして読めるので、そこから時間軸を作る。
    /// </summary>
    public static class TrendMath
    {
        /// <summary>
        /// 既定の不感帯。current は target へ連続的に補間されるので、
        /// 厳密比較だと常に Rising か Falling になり矢印が意味を失う。
        /// </summary>
        public const float DefaultDeadband = 0.02f;

        public static Trend Of(float current, float target, float deadband)
        {
            // 破損値で矢印が嘘をつかないこと。NaN の比較は全て false になるため明示的に弾く。
            if (float.IsNaN(current) || float.IsNaN(target)) return Trend.Steady;
            if (float.IsInfinity(current) || float.IsInfinity(target)) return Trend.Steady;

            float band = deadband > 0f ? deadband : 0f;
            float diff = target - current;

            // 境界はちょうど band のとき Steady 側へ倒す（ちらつきを減らす）。
            if (diff > band) return Trend.Rising;
            if (diff < -band) return Trend.Falling;
            return Trend.Steady;
        }
    }
}
```

- [ ] **Step 4: HazardLevel を実装する**

`src/DisasterPlus/Core/Forecast/HazardLevel.cs`:

```csharp
namespace DisasterPlus.Core.Forecast
{
    /// <summary>
    /// ハザード値（byte 0-255）を表示段階とバー文字列にする。
    /// バニラは色でしか見せないので、段階と数値にするのが本機能の付加価値。
    /// </summary>
    public static class HazardLevel
    {
        public const int Steps = 10;

        /// <summary>
        /// バーは ASCII のみで組む。CS の UI フォントに罫線素片（▓ ░）がある保証は無く、
        /// 無ければ豆腐になる。見た目より確実に出ることを優先する。
        /// </summary>
        public const char FilledChar = '#';
        public const char EmptyChar = '-';

        /// <summary>0-255 を 0-Steps に写す。単調増加。</summary>
        public static int StepOf(byte hazard)
        {
            // 255 でちょうど Steps になるよう切り上げ側に寄せず、整数除算で素直に割る。
            // 255 * Steps / 255 == Steps なので端は両方とも正確に出る。
            return hazard * Steps / 255;
        }

        /// <summary>長さ Steps のバー。埋まった数は StepOf と一致する。</summary>
        public static string BarOf(byte hazard)
        {
            int filled = StepOf(hazard);
            var sb = new System.Text.StringBuilder(Steps);
            for (int i = 0; i < Steps; i++) sb.Append(i < filled ? FilledChar : EmptyChar);
            return sb.ToString();
        }
    }
}
```

- [ ] **Step 5: WindDirection を実装する**

`src/DisasterPlus/Core/Forecast/WindDirection.cs`:

```csharp
namespace DisasterPlus.Core.Forecast
{
    /// <summary>角度を 16 方位のラベルにする。</summary>
    public static class WindDirection
    {
        private const int Sectors = 16;
        private const float SectorWidth = 360f / Sectors;   // 22.5

        /// <summary>
        /// ラベルは static readonly 配列で持ってよい。方位略号は英語固定で、
        /// ローカライズ対象ではないため（言語で凍結する問題が起きない）。
        /// </summary>
        private static readonly string[] Labels =
        {
            "N", "NNE", "NE", "ENE", "E", "ESE", "SE", "SSE",
            "S", "SSW", "SW", "WSW", "W", "WNW", "NW", "NNW",
        };

        public static string LabelOf(float degrees)
        {
            if (float.IsNaN(degrees) || float.IsInfinity(degrees)) return "?";

            // 負の角度と 360 超えの両方を 0-360 に畳む。
            float d = degrees % 360f;
            if (d < 0f) d += 360f;

            // セクタ中心を境界にするため半セクタ分ずらしてから割る。
            int index = (int)((d + SectorWidth * 0.5f) / SectorWidth);
            if (index >= Sectors) index -= Sectors;
            if (index < 0) index = 0;

            return Labels[index];
        }
    }
}
```

- [ ] **Step 6: テストが通ることを確認する**

```bash
dotnet test tests/DisasterPlus.Core.Tests/DisasterPlus.Core.Tests.csproj
```

Expected: 140 件すべて PASS（既存 116 件 + 新規 24 件）

- [ ] **Step 7: コミット**

```bash
git add src/DisasterPlus/Core/Forecast tests/DisasterPlus.Core.Tests/Forecast
git commit -m "feat: 予報の傾向判定・ハザード段階・風向ラベル"
```

---

## Task 2: 空き位置の走査（ボタンが他MODと重ならないように）

**ユーザーからの明示要件。** 固定座標は必ずいつか衝突する。

**Files:**
- Create: `src/DisasterPlus/Game/UI/FreeSlotFinder.cs`

**Interfaces:**
- Consumes: `Log`
- Produces:
  - `FreeSlotFinder` — `static UnityEngine.Vector2 Find(UnityEngine.Vector2 preferred, UnityEngine.Vector2 size, float stepY, int maxTries, out bool foundFree)`

**方式:** `UIView.GetAView()` から全 `UIComponent` を走査し、`absolutePosition` と `size` で矩形を作って重なりを判定。既定位置から縦にずらし、可視要素と重ならない最初の位置を返す。

**重要:** `isVisible` が false の要素は無視する。CS は UI テンプレートを非表示のまま常駐させるので、それを避けると空きが見つからない。

- [ ] **Step 1: 実装する**

`src/DisasterPlus/Game/UI/FreeSlotFinder.cs`:

```csharp
using ColossalFramework.UI;
using UnityEngine;

namespace DisasterPlus.Game
{
    /// <summary>
    /// 他 MOD やバニラのボタンと重ならない位置を実行時に探す。
    ///
    /// CS1 は左上に MOD のボタンが積み上がるのが慣習で、固定座標は必ずいつか衝突する。
    /// ②以降の機能も同じ問題を持つので、ここに置いて共用する。
    /// </summary>
    public static class FreeSlotFinder
    {
        /// <summary>
        /// preferred から下方向へ stepY ずつずらし、可視要素と重ならない最初の位置を返す。
        /// 見つからなければ preferred を返し foundFree=false にする
        /// （隠れて見つからないより、見えて重なる方がマシ）。
        /// </summary>
        public static Vector2 Find(Vector2 preferred, Vector2 size, float stepY,
                                   int maxTries, out bool foundFree)
        {
            foundFree = false;
            try
            {
                var view = UIView.GetAView();
                if (view == null) return preferred;

                var all = view.GetComponentsInChildren<UIComponent>();
                if (all == null || all.Length == 0) { foundFree = true; return preferred; }

                for (int attempt = 0; attempt < maxTries; attempt++)
                {
                    var candidate = new Vector2(preferred.x, preferred.y + stepY * attempt);
                    if (!OverlapsAny(all, candidate, size))
                    {
                        foundFree = true;
                        return candidate;
                    }
                }

                Log.Warn("no free UI slot found after " + maxTries
                         + " tries; placing the button at the preferred position (it may overlap)");
                return preferred;
            }
            catch (System.Exception e)
            {
                Log.Error("free slot search failed", e);
                return preferred;
            }
        }

        private static bool OverlapsAny(UIComponent[] all, Vector2 pos, Vector2 size)
        {
            for (int i = 0; i < all.Length; i++)
            {
                var c = all[i];
                if (c == null) continue;

                // 非表示の要素は無視する。CS は UI テンプレートを非表示のまま
                // 常駐させるので、これを数えると空きが永久に見つからない。
                if (!c.isVisible) continue;

                // 我々自身のボタンは無視する（再配置のたびに自分と衝突しないように）。
                if (c.name != null && c.name.StartsWith("DisasterPlus")) continue;

                Vector2 cp = c.absolutePosition;
                Vector2 cs = c.size;
                if (cs.x <= 0f || cs.y <= 0f) continue;

                bool separated = pos.x + size.x <= cp.x
                              || cp.x + cs.x <= pos.x
                              || pos.y + size.y <= cp.y
                              || cp.y + cs.y <= pos.y;
                if (!separated) return true;
            }
            return false;
        }
    }
}
```

- [ ] **Step 2: ビルドする**

```bash
powershell -ExecutionPolicy Bypass -File build.ps1
```

Expected: 警告 0。`UIView.GetAView()` は static、`UIComponent` の `absolutePosition` / `size` / `isVisible` / `name` は実測済み。`GetComponentsInChildren<UIComponent>()` が解決しない場合は **推測で代替を書かず**、`docs/tools/ilload.ps1` で `ColossalManaged.dll` を確認して報告すること。

- [ ] **Step 3: コミット**

```bash
git add src/DisasterPlus/Game/UI/FreeSlotFinder.cs
git commit -m "feat: 他MODと重ならないUI位置の走査"
```

---

## Task 3: 天候の読み取りとスナップショット

**Files:**
- Create: `src/DisasterPlus/Core/Forecast/ForecastReading.cs`
- Create: `src/DisasterPlus/Game/Forecast/WeatherSnapshot.cs`
- Create: `src/DisasterPlus/Game/Forecast/WeatherReader.cs`
- Create: `src/DisasterPlus/Game/Forecast/ForecastHub.cs`

**Interfaces:**
- Consumes: `Trend`, `TrendMath`（Task 1）、`Log`
- Produces:
  - `ForecastReading` — `struct`、`readonly float Current`、`readonly float Target`、`readonly Trend Trend`、ctor `ForecastReading(float current, float target, float deadband)`
  - `WeatherSnapshot` — `class`（不変）。`readonly ForecastReading Temperature / Rain / Cloud / Fog`、`readonly float WindDegrees`、`readonly float GroundWetness`、`readonly float LastLightning`、`readonly float DisasterProbability`、`readonly int DisasterCooldown`、`readonly bool Valid`
  - `WeatherReader` — `static WeatherSnapshot Read()`（**sim スレッドから呼ぶ**）
  - `ForecastHub` — `static void Publish(WeatherSnapshot)`、`static WeatherSnapshot Latest { get; }`、`static void Clear()`

**スレッド:** `WeatherReader.Read()` は sim スレッド。`Latest` は main スレッドから読む。`FireWhirlRegistry` / `DiagnosticsHub` と同じ snapshot-then-render。

- [ ] **Step 1: ForecastReading を作る**

`src/DisasterPlus/Core/Forecast/ForecastReading.cs`:

```csharp
namespace DisasterPlus.Core.Forecast
{
    /// <summary>1 つの気象値の現在・目標・傾向。不変。</summary>
    public struct ForecastReading
    {
        public readonly float Current;
        public readonly float Target;
        public readonly Trend Trend;

        public ForecastReading(float current, float target, float deadband)
        {
            Current = current;
            Target = target;
            Trend = TrendMath.Of(current, target, deadband);
        }
    }
}
```

- [ ] **Step 2: WeatherSnapshot を作る**

`src/DisasterPlus/Game/Forecast/WeatherSnapshot.cs`:

```csharp
using DisasterPlus.Core.Forecast;

namespace DisasterPlus.Game
{
    /// <summary>
    /// sim スレッドで作り main スレッドで読む不変スナップショット。
    /// 一度作ったら書き換えない。
    /// </summary>
    public class WeatherSnapshot
    {
        public readonly ForecastReading Temperature;
        public readonly ForecastReading Rain;
        public readonly ForecastReading Cloud;
        public readonly ForecastReading Fog;
        public readonly float WindDegrees;
        public readonly float GroundWetness;
        public readonly float LastLightning;
        public readonly float DisasterProbability;
        public readonly int DisasterCooldown;

        /// <summary>読み取りに成功したか。false ならパネルは「読み取れません」と出す。</summary>
        public readonly bool Valid;

        public WeatherSnapshot(ForecastReading temperature, ForecastReading rain,
                               ForecastReading cloud, ForecastReading fog,
                               float windDegrees, float groundWetness, float lastLightning,
                               float disasterProbability, int disasterCooldown, bool valid)
        {
            Temperature = temperature;
            Rain = rain;
            Cloud = cloud;
            Fog = fog;
            WindDegrees = windDegrees;
            GroundWetness = groundWetness;
            LastLightning = lastLightning;
            DisasterProbability = disasterProbability;
            DisasterCooldown = disasterCooldown;
            Valid = valid;
        }

        public static WeatherSnapshot Invalid()
        {
            var zero = new ForecastReading(0f, 0f, TrendMath.DefaultDeadband);
            return new WeatherSnapshot(zero, zero, zero, zero, 0f, 0f, 0f, 0f, 0, false);
        }
    }
}
```

- [ ] **Step 3: WeatherReader を作る**

`src/DisasterPlus/Game/Forecast/WeatherReader.cs`:

```csharp
using ColossalFramework;
using DisasterPlus.Core.Forecast;

namespace DisasterPlus.Game
{
    /// <summary>
    /// WeatherManager と DisasterManager から現在の気象と災害確率を読む。
    ///
    /// **sim スレッドから呼ぶこと。** これらのマネージャはシミュレーションが所有する。
    ///
    /// current と target の両方を読むのが本機能の要。バニラは hazard（静的な危険度）
    /// しか見せず、「今どちらへ向かっているか」を出さない。
    /// </summary>
    public static class WeatherReader
    {
        public static WeatherSnapshot Read()
        {
            try
            {
                if (!Singleton<WeatherManager>.exists) return WeatherSnapshot.Invalid();
                var w = Singleton<WeatherManager>.instance;

                float band = TrendMath.DefaultDeadband;

                // 気温はスケールが大きいので不感帯を広げる（0.02 度では常に変化扱いになる）。
                var temperature = new ForecastReading(
                    w.m_currentTemperature, w.m_targetTemperature, 0.5f);
                var rain = new ForecastReading(w.m_currentRain, w.m_targetRain, band);
                var cloud = new ForecastReading(w.m_currentCloud, w.m_targetCloud, band);
                var fog = new ForecastReading(w.m_currentFog, w.m_targetFog, band);

                float probability = 0f;
                int cooldown = 0;
                if (Singleton<DisasterManager>.exists)
                {
                    var d = Singleton<DisasterManager>.instance;
                    probability = d.m_randomDisastersProbability;
                    cooldown = d.m_randomDisasterCooldown;
                }

                return new WeatherSnapshot(
                    temperature, rain, cloud, fog,
                    w.m_windDirection, w.m_groundWetness, w.m_lastLightningIntensity,
                    probability, cooldown, true);
            }
            catch (System.Exception e)
            {
                Log.Error("weather read failed", e);
                return WeatherSnapshot.Invalid();
            }
        }
    }
}
```

**注意:** `m_randomDisastersProbability` と `m_randomDisasterCooldown` の実際の型を
`docs/tools/ilload.ps1` で確認し、`float` / `int` と違う場合は合わせること。**推測しない。**

- [ ] **Step 4: ForecastHub を作る**

`src/DisasterPlus/Game/Forecast/ForecastHub.cs`:

```csharp
namespace DisasterPlus.Game
{
    /// <summary>
    /// sim スレッドが Publish し main スレッドが Latest を読む。
    /// DiagnosticsHub と同じ形（net35 に Concurrent は無いので素の lock）。
    /// WeatherSnapshot は不変なので参照を渡すだけで安全。
    /// </summary>
    public static class ForecastHub
    {
        private static readonly object _gate = new object();
        private static WeatherSnapshot _latest;

        public static void Publish(WeatherSnapshot snapshot)
        {
            lock (_gate) { _latest = snapshot; }
        }

        /// <summary>まだ publish されていなければ null。呼び出し側で判定すること。</summary>
        public static WeatherSnapshot Latest
        {
            get { lock (_gate) { return _latest; } }
        }

        public static void Clear()
        {
            lock (_gate) { _latest = null; }
        }
    }
}
```

- [ ] **Step 5: ビルドとテスト**

```bash
powershell -ExecutionPolicy Bypass -File build.ps1
dotnet test tests/DisasterPlus.Core.Tests/DisasterPlus.Core.Tests.csproj
```

Expected: 警告 0、Core 140 件緑（`ForecastReading` は Core だが専用テストは不要 — `TrendMath` が全て覆っている）。

- [ ] **Step 6: コミット**

```bash
git add src/DisasterPlus && git commit -m "feat: 気象と災害確率の読み取りとスナップショット受け渡し"
```

---

## Task 4: ハザード読み取りと情報ビュー切替

**Files:**
- Create: `src/DisasterPlus/Game/Forecast/HazardMapReader.cs`
- Create: `src/DisasterPlus/Game/Forecast/InfoModeSwitch.cs`

**Interfaces:**
- Consumes: `Log`
- Produces:
  - `HazardMapReader` — `static byte SampleAt(UnityEngine.Vector3 worldPos, InfoManager.SubInfoMode subMode, out bool ok)`
  - `InfoModeSwitch` — `static void ShowHazard(InfoManager.SubInfoMode subMode)`、`static void Clear()`、`static bool IsShowingHazard { get; }`

**これが「自前でヒートマップを描かない」の実体。** `SetCurrentMode` は public なので 1 行で済む。

- [ ] **Step 1: 呼び出し規約を IL で確認する**

`docs/tools/ilload.ps1` で次を確認してから実装する。**推測で書かない。**

- `DisasterManager.SampleDisasterHazardMap` の完全なシグネチャ（引数の型と順序、戻り値）
- そのメソッドが sim スレッド専用かどうか（本体が書き込みを行うか読み取りだけか）
- `InfoManager.SetCurrentMode` が main スレッド専用か（UI に触るなら main）

読み取りだけなら main スレッドから呼んでよい。書き込みがあるなら sim 側で読んでスナップショットに載せること。**判明した事実を報告に書くこと。**

- [ ] **Step 2: InfoModeSwitch を作る**

`src/DisasterPlus/Game/Forecast/InfoModeSwitch.cs`:

```csharp
using ColossalFramework;

namespace DisasterPlus.Game
{
    /// <summary>
    /// バニラの災害ハザード情報ビューへの切替。
    ///
    /// ヒートマップは自前で描かない。IL 実測でバニラに完全な実装があることが分かっている
    /// （ThunderStormAI.UpdateHazardMap 237命令 / TornadoAI 335命令、
    /// InfoMode.DisasterHazard、SubInfoMode.LightningHazard / TornadoHazard）。
    /// SetCurrentMode は public なので、ここは薄いラッパーで足りる。
    ///
    /// **main スレッドから呼ぶこと**（UI に触る）。
    /// </summary>
    public static class InfoModeSwitch
    {
        public static bool IsShowingHazard
        {
            get
            {
                try
                {
                    if (!Singleton<InfoManager>.exists) return false;
                    return Singleton<InfoManager>.instance.CurrentMode
                           == InfoManager.InfoMode.DisasterHazard;
                }
                catch { return false; }
            }
        }

        public static void ShowHazard(InfoManager.SubInfoMode subMode)
        {
            try
            {
                if (!Singleton<InfoManager>.exists) { Log.Warn("InfoManager not ready"); return; }
                Singleton<InfoManager>.instance.SetCurrentMode(
                    InfoManager.InfoMode.DisasterHazard, subMode);
            }
            catch (System.Exception e)
            {
                Log.Error("failed to switch to the disaster hazard info view", e);
            }
        }

        /// <summary>通常表示に戻す。</summary>
        public static void Clear()
        {
            try
            {
                if (!Singleton<InfoManager>.exists) return;
                Singleton<InfoManager>.instance.SetCurrentMode(
                    InfoManager.InfoMode.None, InfoManager.SubInfoMode.Default);
            }
            catch (System.Exception e)
            {
                Log.Error("failed to clear the info view", e);
            }
        }
    }
}
```

**注意:** `InfoManager.InfoMode.None` が存在するかを IL で確認すること。無ければ実際の既定値に合わせる。

- [ ] **Step 3: HazardMapReader を作る**

Step 1 で確認したシグネチャに合わせて実装する。形は次のとおり。

```csharp
using ColossalFramework;
using UnityEngine;

namespace DisasterPlus.Game
{
    /// <summary>
    /// 地点のハザード値を読む。バニラは色でしか見せないので、
    /// 数値にすること自体が本機能の付加価値になる。
    /// </summary>
    public static class HazardMapReader
    {
        public static byte SampleAt(Vector3 worldPos, InfoManager.SubInfoMode subMode, out bool ok)
        {
            ok = false;
            try
            {
                if (!Singleton<DisasterManager>.exists) return 0;
                // Step 1 で確認した実シグネチャに合わせる。
                byte value = Singleton<DisasterManager>.instance
                    .SampleDisasterHazardMap(worldPos, subMode);
                ok = true;
                return value;
            }
            catch (System.Exception e)
            {
                Log.Error("hazard sample failed", e);
                return 0;
            }
        }
    }
}
```

- [ ] **Step 4: ビルドする**

```bash
powershell -ExecutionPolicy Bypass -File build.ps1
```

Expected: 警告 0。

- [ ] **Step 5: コミット**

```bash
git add src/DisasterPlus && git commit -m "feat: ハザード値の読み取りとバニラ情報ビューへの切替"
```

---

## Task 5: パネル・ボタン・機能配線・前提検証・設定

**Files:**
- Create: `src/DisasterPlus/Game/Forecast/ForecastPanel.cs`
- Create: `src/DisasterPlus/Game/UI/ForecastPanelButton.cs`
- Create: `src/DisasterPlus/Game/Forecast/ForecastFeature.cs`
- Modify: `src/DisasterPlus/Game/Common/DisasterPlusLoading.cs`（機能登録）
- Modify: `src/DisasterPlus/Game/Diagnostics/Assumptions.cs`（予報ぶんの前提）
- Modify: `src/DisasterPlus/Game/ModSettings.cs`（設定キー）
- Modify: `src/DisasterPlus/Game/Mod.cs`（設定グループ・Forecast ログチャンネル）
- Modify: `src/DisasterPlus/Game/Localization/Strings.cs`、`Locales/en.txt`、`Locales/ja.txt`

**Interfaces:**
- Consumes: 全て
- Produces: `ForecastFeature : IDisasterFeature`、`ForecastPanel`（`Show()`/`Hide()`/`Toggle()`/`IsVisible`/`Destroy()`）、`ForecastPanelButton`（`Install()`/`Remove()`/`Tick()`）

### 5.1 設定キー（既存を触らない）

```csharp
ForecastEnabled    = new SavedBool("forecastEnabled", FileName, true, true);
ForecastButtonX    = new SavedInt("forecastButtonX", FileName, -1, true);   // -1 = 未決定
ForecastButtonY    = new SavedInt("forecastButtonY", FileName, -1, true);
```

保存済みの座標があればそれを使い、無ければ `FreeSlotFinder` で探して保存する。
設定画面の「ボタン位置をリセット」で両方を -1 に戻す。

### 5.2 パネルの内容

設計書 §4 の表示。**ただし §4.2 の訂正を必ず読むこと** — ハザード値は種別ごとに同時取得できない。
`m_hazardAmount` は単一の共有グリッドで、**いま表示中のサブモード 1 種ぶん**しか保持していない。
パネルは「現在表示中のハザード」を 1 つだけ出し、ハザードビューが出ていなければ数値を出さない。
**表示していない種別のラベルを付けた数値を出さないこと。**

日英ローカライズ対象。傾向は矢印文字ではなく
`Strings.TrendRising` / `TrendFalling` / `TrendSteady`（既定値 `"up"` / `"down"` / `"steady"`）で表す
— **矢印記号がフォントに無い可能性があるため**、`HazardLevel` のバーと同じ判断。

**「あと何時間で来る」と書かない。** バニラの発生判定は乱数で時刻を予言できない。
出すのは傾向・相対的な高低・クールダウン中かどうかに留める。根拠のない数字は出さない。

### 5.3 前提検証（`Assumptions` に追加）

| 名前 | 破れたときの影響 |
|---|---|
| `InfoManager.SetCurrentMode is resolvable` | ハザードマップへ切り替えられない |
| `SubInfoMode.LightningHazard / TornadoHazard exist` | 落雷・竜巻のハザード表示ができない |
| `ThunderStormAI/TornadoAI.UpdateHazardMap exist` | ハザードマップが埋まらず、表示しても空になる |
| `WeatherManager current/target fields are resolvable` | 傾向が出せない（本機能の中核が死ぬ） |

いずれもリフレクションで解決可否を見る。**既存の検証と同じく、個別に try/catch し、
判定できないものは FAIL とする（偽 PASS を出さない）。**

### 5.4 スレッド境界

- `ForecastFeature.OnSimulationTick` → `WeatherReader.Read()` → `ForecastHub.Publish()`（sim）
- `ForecastFeature.OnMainThreadUpdate` → パネル更新・ボタン設置（main）
- `OnLevelUnloading` → `ForecastHub.Clear()`、パネルとボタンの破棄

### 5.5 診断

`WriteDiagnostics` に出す: 有効/無効、スナップショットの有無、雨・雲・気温の現在値と傾向、
ボタン位置と空き探索の成否、情報ビューが今ハザード表示中か。

- [ ] **Step 1: `Strings` に文字列を足し、3 つのロケール源を一致させる**

追加するキー（既定値は英語）:

| フィールド | 英語既定値 |
|---|---|
| `GroupForecast` | `Weather forecast` |
| `ForecastEnabled` | `Enable the forecast panel` |
| `ForecastResetButton` | `Reset the forecast button position` |
| `ForecastTitle` | `Weather forecast` |
| `ForecastTemperature` | `Temperature` |
| `ForecastRain` | `Rain` |
| `ForecastCloud` | `Cloud` |
| `ForecastWind` | `Wind` |
| `ForecastLightning` | `Lightning` |
| `ForecastTornado` | `Tornado` |
| `ForecastShowOnMap` | `Show on map` |
| `ForecastAtCursor` | `Hazard at cursor` |
| `ForecastUnavailable` | `Weather data unavailable` |
| `ForecastNdrNote` | `Natural Disasters Renewal governs disaster occurrence.` |
| `TrendRising` | `up` |
| `TrendFalling` | `down` |
| `TrendSteady` | `steady` |
| `LogChannelForecast` | `Forecast` |

26 → 44 件になる。`ja.txt` に訳を書き、`tools/GenerateLocaleTemplate.ps1` で `en.txt` を再生成し、
**3 つの集合が一致していることを確認する**。

- [ ] **Step 2: 設定・ログチャンネル・前提検証を足す**

`ModSettings` に 5.1 のキー、`Mod.OnSettingsUI` に予報グループ（有効化チェックボックスと
ボタン位置リセット）、`LogChannel.Forecast`（= 4、定義済み）のチェックボックス、
`Assumptions` に 5.3 の 4 項目。

**`LogChannel` は常に完全修飾する**（`Assembly-CSharp` がグローバル名前空間に同名の enum を持つ）。

- [ ] **Step 3: パネル・ボタン・機能を作り、配線する**

`DisasterPlusLoading.OnLevelLoaded` の機能登録は `FeatureHost.LevelLoaded()` の**前**に行う
（`_features` は同期されておらず、ディスパッチ開始前に揃っている必要がある）。

- [ ] **Step 4: ビルドとテスト**

```bash
powershell -ExecutionPolicy Bypass -File build.ps1
dotnet test tests/DisasterPlus.Core.Tests/DisasterPlus.Core.Tests.csproj
```

Expected: 警告 0、Core 140 件緑、ロケール 44/44/44 一致、BOM なし。

- [ ] **Step 5: コミット**

```bash
git add -A && git commit -m "feat: 予報パネル・ボタン配置・前提検証・設定"
```

---

## 実機確認（`docs/playtest-checklist.md` に追記する）

1. **ボタンがバニラや他MODのボタンと重ならない位置に出る**（ユーザー要件）
2. ボタンでパネルが開閉する
3. 気温・雨・雲・風向と傾向が出る。ゲーム内時間を進めると傾向が変わる
4. **「マップに表示」でバニラのハザードヒートマップに切り替わる**
5. カーソル位置のハザード数値が出る
6. NDR 併用時に注記が出る
7. 起動ログの `ASSUMPTIONS` に予報ぶんの 4 項目が出る
8. **2 つ目の都市をロードしても正常に動く**
9. バーと傾向のラベルが**豆腐にならない**（ASCII のみにした理由の確認）

