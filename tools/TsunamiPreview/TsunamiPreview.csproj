<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>disable</Nullable>
    <AssemblyName>TsunamiPreview</AssemblyName>
    <RootNamespace>DisasterPlus.Tools.TsunamiPreview</RootNamespace>
    <LangVersion>7.3</LangVersion>
  </PropertyGroup>

  <!-- Core の実物をそのままコンパイルする。**書き直した近似ではない。** -->
  <ItemGroup>
    <Compile Include="..\..\src\DisasterPlus\Core\**\*.cs" />
    <Compile Include="..\Shared\Png.cs" />
  </ItemGroup>

</Project>
