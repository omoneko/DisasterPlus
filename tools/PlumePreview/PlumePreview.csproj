<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>disable</Nullable>
    <AssemblyName>PlumePreview</AssemblyName>
    <RootNamespace>DisasterPlus.Tools.PlumePreview</RootNamespace>
    <LangVersion>7.3</LangVersion>
  </PropertyGroup>

  <!-- Compile the real Core sources as they are. **Not a rewritten approximation.** -->
  <ItemGroup>
    <Compile Include="..\..\src\DisasterPlus\Core\**\*.cs" />
    <Compile Include="..\Shared\Png.cs" />
  </ItemGroup>

</Project>
